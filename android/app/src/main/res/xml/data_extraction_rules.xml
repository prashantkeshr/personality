<?xml version="1.0" encoding="utf-8"?>
<!-- Personal data is never included in Android cloud backup or device
     transfer. The encrypted database key lives in the Keystore and cannot be
     migrated, so a copied database would be unreadable anyway. User-controlled
     encrypted backup is handled by the app itself (Phase 13). -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="external" path="." />
    </device-transfer>
</data-extraction-rules>
